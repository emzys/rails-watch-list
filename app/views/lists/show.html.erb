<h1><%= @list.name %> list</h1>

<div class="bookmarks m-3">
  <% if @list.bookmarks.any? %>
    <h2>Bookmarks ( <%= @list.bookmarks.count %> )</h2>
    <% @list.bookmarks.each do |bookmark| %>
      <hr>
      <p> <%= bookmark.comment %> </p>
    <% end %>
  <% end %>
</div>

<%= link_to "Add bookmark", new_list_bookmark_path(@list) %>
